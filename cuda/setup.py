import setuptools

setuptools.setup(
    name="cuda", # Replace with your own username
    packages=setuptools.find_packages(),
    classifiers=[
        "Programming Language :: Python :: 3",
    ],
    python_requires='>=3.7',
)